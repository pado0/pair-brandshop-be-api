spring:
  datasource:
    url: jdbc:h2:tcp://localhost/~/brandshop
    username: sa
    password:
    driver-class-name: org.h2.Driver

  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        #      show_sql: true
        format_sql: true
        default_batch_fetch_size: 100

  security:
    user:
      name: user
      password: 1111

  jwt:
    secret: pepega52483285724012312314w23234324323535233
spring.jpa.properties.hibernate.use_sql_comments: true

#spring.jackson.serialization.fail-on-empty-beans: false :
# 사용시 hibernate5module과 같은역할 -> @JsonIgnore와 BatchSize로 해결 함



logging.level:
  org.hibernate.SQL: debug



# org.hibernate.type: trace